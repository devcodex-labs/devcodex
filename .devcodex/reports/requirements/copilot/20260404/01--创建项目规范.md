> **项目**: DevCodex
> **类型**: dev
> **子类型**: init（项目规范初始化）
> **创建日期**: 2026-04-04
> **Agent**: copilot
> **状态**: 已完成

# 01 — 创建项目规范

## 执行内容

### 1. Agent tools 字段修复

| 旧名称（无效） | 新名称（官方 tool set） |
|--------------|----------------------|
| `editFiles` | `edit` |
| `runCommands` | `execute` |
| `codebase` / `usages` | `search` |
| `problems` | `read` |
| `fetch` | `web/fetch` |

**验证**：修改 `agents/devcodex.agent.md` → `devcodex update` → 70 files updated ✅

### 2. 项目规范创建（.devcodex/profile/）

| 文件 | 内容 | 行数 |
|------|------|:----:|
| `README.md` | Profile 索引 + 快速参考 | ~35 |
| `01-项目信息.md` | 技术栈、开发模式、版本策略 | ~85 |
| `02-架构约束.md` | 目录结构、模块职责、CLI 流程 | ~80 |
| `03-代码风格.md` | JS 规范、Markdown 规范、禁止事项 | ~90 |

### 3. 开发模式说明补充

在 `01-项目信息.md` 新增"当前开发模式"章节，明确：
- 开发状态：`npm link` 激活（本地开发模式）
- 源码 → 工作区同步命令：`devcodex update`
- 路径对应关系表（5 个目录映射）
- 修改后需重新开 Chat 会话触发文件加载

## 合规自查结论

| 检查项 | 状态 | 说明 |
|--------|:----:|------|
| FC1 记忆字段完整 | ✅ | 本次补写 |
| FC2 报告文件写入 | ✅ | 本次补写 |
| FC4 文件名双横杠 | ✅ | `01--创建项目规范.md` |
| FC5 产物路径输出 | ✅ | 回复末尾输出 |
| FC6 新建 .md ≤500 行 | ✅ | 最大 ~90 行 |
| SC4 关联文件同步 | ✅ | devcodex update 已同步 |

### 建议（三项验证）

**建议：将 `.devcodex/profile/` 纳入 `devcodex update` 排除列表**
- **合理性**：profile 是项目级私有配置，不应被 `init --force` 覆盖
- **可实施性**：在 `index.js` SOURCES 中排除 `.devcodex/` 目录即可（约 1 行）
- **收益**：防止用户运行 `devcodex update` 时意外覆盖自定义的项目规范
