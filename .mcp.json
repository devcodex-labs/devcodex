{
  "mcpServers": {
    "devcodex-memory": {
      "type": "stdio",
      "command": "node",
      "args": [
        "mcp/memory-server.js",
        "."
      ],
      "_note": "Package-local development manifest; installed Claude projects receive .claude/mcp paths from the CLI."
    },
    "devcodex-profile": {
      "type": "stdio",
      "command": "node",
      "args": [
        "mcp/profile-server.js",
        "."
      ],
      "_note": "Package-local development manifest; installed Claude projects receive .claude/mcp paths from the CLI."
    }
  }
}
