{
  "$schema": "https://json.schemastore.org/mcp-servers.json",
  "_comment": "Placeholder config (v1.0.x). MCP servers will be enabled in future version.",
  "servers": {
    "devcodex-memory": {
      "_comment": "Planned (v1.1): mcp/memory-server.js does not exist yet. Enable after implementation.",
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/mcp/memory-server.js"],
      "disabled": true,
      "tools": ["memory_read", "memory_write", "memory_list"],
      "resources": ["memory://sessions/**"]
    },
    "devcodex-violations": {
      "_comment": "Planned (v1.1): mcp/violations-server.js does not exist yet. Enable after implementation.",
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/mcp/violations-server.js"],
      "disabled": true,
      "tools": ["violation_log", "violation_query", "violation_stats"]
    },
    "devcodex-auth": {
      "_comment": "Planned (v1.1): MCP protocol layer for auth service. Currently auth.devcodex.dev only exposes /validate and /auth/* REST endpoints.",
      "type": "http",
      "url": "https://auth.devcodex.dev/mcp",
      "disabled": true,
      "tools": ["token_validate", "token_refresh", "tier_check"]
    }
  }
}
