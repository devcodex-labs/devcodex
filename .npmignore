# Dev tooling (not needed by plugin users)
node_modules/
*.tgz

# Documentation website (separate build artifact)
website/

# Archive snapshot
v0.03/

# Changelogs detail (CHANGELOG.md summary is enough for users)
changelogs/

# Dev workspace artifacts — defense-in-depth; primary control is package.json "files" whitelist
.devcodex/
assets/hooks/
scripts/
.github/workflows/
