# v1.11.6 — 治理台账长尾与发布守门补强

> 发布日期：2026-06-01

## Highlights

- 收口治理台账剩余长尾：新增并验证 Backlog Intake 真相复核、台账状态回写闭环与 active-root 时间线守门，降低假 open、错误统计和重复建需求风险。
- 补齐发布前审查能力：新增 `audit-release` 与 RL-1~RL-10，覆盖 release readiness、package/plugin 元数据、文档/Profile/website 同步、回滚与发布后验收。
- 强化跨宿主恢复与产物交付：新增 `ArtifactLinkSet` 客户端兼容矩阵、MCP fallback 证据链，以及 Codex `PreCompact` runtime 兜底。
- 补齐安全与文档治理长尾：S02 受控私有例外模型、API `.http` 标准变量、`changelogs/releases/` 分层结构和 Profile Freshness Check 已纳入规范与探针。

## Changed

- `V49` 现在会扫描 active-root `violations.md` / `pending-fixes.md` 的 `VL/PF` 关闭链时间顺序，要求 `登记时间 <= 修复时间 <= 验证时间/关闭时间`。
- `instructions.md`、`instructions/01b-record-router.instructions.md`、`skills/spec-governance/SKILL.md` 与 `data/templates/*` 已同步台账关闭时间线规则。
- `release-verification`、`audit-release`、README、website 与 package metadata gate 已形成发布审查/发布验证职责边界。
- `codex/hooks.json`、Hook runtime、client contract tests 与 MCP server tests 已覆盖 Codex PreCompact、ArtifactLinkSet 与 `mcpFallback=used`。

## Added

- `audit-release` Skill 与 RL-1~RL-10 发布前审查维度。
- `V49` active-root ledger chronology check。
- `V50`~`V53` 治理、客户端、compaction、安全与 Profile Freshness 守门探针。
- `scripts/test-client-contracts.js`、`scripts/test-release-metadata.js` 等 targeted checks 已接入主验证链。

## Validation

- `node scripts/test-spec-governance.js`
- `node scripts/validate.js`
- `npm test`
- `npm run test:audit`
- `npm pack --dry-run`
- `npm publish --dry-run --access restricted`
- `git diff --check`
- `node .\devcodex-v1\index.js update`

## Included Changes

- 2026-06-01 `治理台账剩余15项分层收口修复`：收口 Batch 0/A/B/C/D/5，关闭 `PF-014`~`PF-016`、`GR-007` 等治理长尾，并完成发布前审查、客户端 fallback、compaction runtime、安全例外、changelog releases 与 Profile Freshness 的规范/探针/部署闭环。
