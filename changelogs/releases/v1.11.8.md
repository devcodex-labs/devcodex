# v1.11.8 — technical-design 技术方案模板字段补强

> 发布日期：2026-06-02

## Highlights

- 补强 `prompts/technical-design.prompt.md` 的 CP2 技术方案骨架，新增目标架构与模块边界、数据模型 / 状态模型、接口契约矩阵、技术执行流程锚点和需求验收映射。
- 同步 `skills/dev-plan-review`，把目标架构、接口契约、数据/状态模型和需求验收映射纳入 PR-1、PR-2、PR-7 审查与阻断条件。
- 扩展 `scripts/lib/validate-governance-prompts.js` 的 `V13` 语义探针，防止 technical-design 模板缺少关键方案字段时继续通过自动校验。
- 关闭 `PF-019`，将用户指出的 technical-design 模板字段缺口收口为可验证的模板、审查、校验三层闭环。

## Changed

- `prompts/technical-design.prompt.md` 新增 `§2.0 目标架构与模块边界`、契约矩阵、数据/状态模型与 `§7.1 需求验收映射`。
- `skills/dev-plan-review/SKILL.md` 新增 CP2 前置自检、详细方案评审和测试策略评审中的字段完整性要求。
- `scripts/lib/validate-governance-prompts.js` 新增 technical-design 字段完整性 probe。
- `changelogs/unreleased.md` 已归档本轮 technical-design 模板补强条目并复位为空模板。

## Release Gate Evidence

- `OfficialDocsEvidence`: N/A。本轮未新增或升级第三方依赖、框架、SDK、平台 API 或外部模块；变更限定为仓内模板、Skill 与治理校验探针。
- `ProfileImpactCheck`: 命中。发布版本与当前阶段发生变化，已同步 active profile `01-项目信息.md` 的当前版本、当前阶段和发布优先级口径。

## Validation

- `git diff --check`
- `node .\devcodex-v1\index.js update`
- `node scripts\validate.js`
- `node scripts\test-spec-governance.js`
- `npm test`
- `npm run test:audit`
- `npm pack --dry-run`
- `npm publish --dry-run --access restricted`
- pack install smoke

## Included Changes

- 2026-06-02 `technical-design 模板字段补强`：补齐架构、数据模型、接口契约、流程和验收映射字段，并同步 plan-review 与 V13 自动探针。
