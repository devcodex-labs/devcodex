# v1.9.3 — 2026-05-20

## 🔧 PATCH

### 修复

- **CP gate 全局阻断风险消除**：`hooks/_runtime/lifecycle.cjs` `findIncompleteRequirement` 增加 `.archived` 标记跳过机制；历史已完成需求可在其目录下 `touch .archived` 归档，避免长期堆积的历史需求阻断新会话的所有 dev 工作流（事故源：v1.9.2 发版收口时本会话亲测被旧需求 deny）
- **报告头部规范回填**：`reports/audit/claude-code/20260520/03--*.md` 头部补齐 `> **项目** / **类型** / **创建日期** / **Agent** / **状态** / **审查目标类型**` 等强制字段（FC2/16-report 要求）
- **L 级建议补三列验证**：报告 03 中 L1~L5 建议补全 `合理性 / 可实施性 / 收益` 列（SC1/V2 要求）
- **v1.9.2 git tag 补打**：补打 `v1.9.2` tag 并推送至 origin（02-output-paths Git Tag 发布规范要求）

### 测试

- `scripts/test-hooks-runtime.js` 新增 archive 跳过用例：先创建未完成需求验证 deny，再 `touch .archived` 验证 allow

### 文档

- `instructions/02-output-paths.instructions.md` 目录规则表新增"需求归档"行，文档化 `.archived` 标记用法

## 影响

- 🟢 **行为兼容**：未 `touch .archived` 的需求行为与 v1.9.2 完全一致
- 🟢 **新增能力**：旧需求归档无需 CLI 子命令，单文件 `touch` 即可
