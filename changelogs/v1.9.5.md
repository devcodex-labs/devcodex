# v1.9.5 — 规范层 + 工程层闭环（FC7 + PCV-6/7 + V8 扩展 + V9/V10/V11）

> 发布日期：2026-05-21
> 上游：v1.9.4（防漂移机制基线）
> 主题：将 v1.9.4 audit R4 暴露的 8 个工程缺陷（F-001~F-013 中标注 `v1.9.5-candidate` 项 + audit 元循环新发现）合并落地；同步将 PI-005（决策必带推荐）、PI-006（V8 ≠ CRS）规范化进 Instructions/Skills。

## 一、规范层（5 文件）

### 1.1 `instructions/17-compliance.instructions.md` — 新增 FC7
- FC 表第 7 行：所有 AskUserQuestion / 多选项决策节点必须有且仅有 1 个 🟢 推荐项（首位置 + 标签含 `(推荐)`） + 一句话推荐理由（实证依据/风险权衡/性价比）。
- 适用范围：CP1/CP2/CP3 范围选择、方案对比、audit/analyze 报告 §决策点。
- 关联：[PI-005](../data/process-improvements.md#pi-005)。

### 1.2 `skills/cp-gate/SKILL.md` — 新增 §用户决策节点
- AskUserQuestion JSON 调用模板：1 推荐项首位 + 描述以"推荐理由："开头；其他选项列 "代价/适用场景"。
- 报告/Markdown 决策点 Markdown 表模板。
- 校验：FC7 + scripts/validate.js V11。

### 1.3 `skills/audit-session/SKILL.md` — schema 扩展
- 新增字段：`findings[].category | fixPlan | fixCommit`、顶层 `regressionProbes[]`（`{findingId, scanCmd, expectedMatches, lastVerifiedRound, lastVerifiedAt}`）、`r{N}Probes[]`、`remoteReleased`、`lastCheckpoint.reason` 扩充枚举、`linkedRelease`。
- 新增 §v1.9.4+ schema 字段说明表（含 v1.9.5 `regressionProbes`）。

### 1.4 `skills/audit-common/SKILL.md` — CRS + PCV 强化
- §关联文件发现 step 3 追加 PI-006 提示：「V8 与 CRS 职责不可互替」。
- §收敛后汇总验证（PCV）表新增 PCV-6（回归复扫）+ PCV-7（收敛门禁强制 `crsPassed && pcvPassed && zeroFindingStreak>=3`）。

### 1.5 `.devcodex/.maintainer-state/process-improvements.md` — PI-006 落地
- 新增 PI-006：「V8 自动化 ≠ CRS 全覆盖」，建议 audit-common 文档化（已在 1.4 落地）。

## 二、工程层（10 文件）

### 2.1 `scripts/validate.js` — V8 扩展 + V9/V10/V11 新增 + F-012/F-013
- **V8 checkPairs**：从 8 → 21 文件（覆盖 33% → ~95%；含 instructions/skills/hooks + CLAUDE.md `.claude/` 单边同步）。F-005 关闭。
- **V9 日期格式**：扫描 instructions/skills/prompts 中非法日期占位（`YYYY/MM/DD`、`MM-DD-YYYY`、`DD-MM-YYYY`、`YYYY.MM.DD`）。
- **V10 回归探针**：读取 `.devcodex/.audit-state/*.json` 的 `regressionProbes[]`，逐项执行 `scanCmd` 与 `expectedMatches` 对比；任一回归 → warning。
- **V11 FC7 决策格式**：扫描 `## 选项` / `**选项**` 块附近是否含 `(推荐)`/🟢 标记 + `推荐理由：` 前缀。
- **F-012**：V6 单次 `npm pack --dry-run --json` 调用（去除二次 `npm pack 2>&1`），CI 提速 1~2s。
- **F-013**：V6/V7 stderr 保留前 8 行（原 `split('\n')[0]` 截断诊断信息）。

### 2.2 `scripts/validate-profile.js` — 新建
- 检查 `.devcodex/profile/` 必需文件 + `config.json` mode/agent/pluginVersion 漂移。
- 退出码：0=OK / 1=error / 2=warnings-only。

### 2.3 `hooks/_runtime/lifecycle.cjs` — F-001 + F-006
- **F-001**：`DEVCODEX_PATH_RE` 从 `/\.devcodex[/\\]|\.claude[/\\]|\.github[/\\]/` 收紧为仅放行 governance 子路径（`.claude|.github/{instructions|skills|hooks|agents|prompts|data}`、`settings.json` 等）。bash 写 `.claude/foo.js` 现在被识别为 source mutation。
- **F-006**：`extractToolPaths()` 从 Bash 命令文本提取路径（重定向 / `tee` / `Set-Content` / `Out-File` / `cp|mv|rm|cat|touch`）。

### 2.4 `index.js` — F-002
- `cmdInitClaude` 新增 `.claude/agents/` legacy 警告（Claude Code 通过 Skill 工具读取 skills/，不使用 agents/）。

### 2.5 `.devcodex/.maintainer-state/gap-registry.md` — F-003
- 补 `## Gap #GAP-017` 标头（原内容散落在 GAP-018 后无标头）。

### 2.6 `scripts/test-hooks-runtime.js` — F-008
- 新增 path-aware 边缘场景：`.claude/foo.js` vs `.claude/instructions/foo.md` 区分 + Bash `cp` 命令路径提取。

## 三、关联 audit findings 状态

| F-NNN | 状态 | 修复点 |
|-------|------|--------|
| F-001 | fixed | hooks/_runtime/lifecycle.cjs L369 |
| F-002 | fixed | index.js cmdInitClaude L444 |
| F-003 | fixed | gap-registry.md 新增 GAP-017 标头 |
| F-005 | fixed | scripts/validate.js V8 checkPairs |
| F-006 | fixed | hooks/_runtime/lifecycle.cjs extractToolPaths |
| F-008 | fixed | scripts/test-hooks-runtime.js |
| F-012 | fixed | scripts/validate.js V6 单次 npm pack |
| F-013 | fixed | scripts/validate.js V6/V7 stderr 8 行 |

> F-004（CLAUDE.md ↔ instructions/ 双向同步 CLI）暂留待 v1.9.6 评估。

## 四、回归门禁

- `node scripts/validate.js`：V1-V11 全 ✅，V8 同步检查 0 stale。
- `node scripts/test-hooks-runtime.js`：含 F-001/F-006/F-008 新增用例，全部通过。
