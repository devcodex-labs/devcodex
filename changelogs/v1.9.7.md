# v1.9.7 — JetBrains 等价兼容 + monorepo 父链强化

> 发布日期：2026-05-21
> 上游：v1.9.6
> 主题：闭环 v1.9.6 延后项 P-001/P-004/P-008，新增 `devcodex doctor` 运行时诊断 CLI。

## 一、问题清单 → 修复映射

| P-NNN | 严重度 | 修复点 | 关联 v1.9.7 文件 |
|-------|:--:|--------|------------------|
| P-001 | 🟡 P1 | JetBrains 等价能力诊断（doctor CLI 输出 mode/agent/instruction-fallback 推荐项） | index.js cmdDoctor |
| P-004 | 🟡 P1 | JetBrains 实测清单内置到 doctor 输出（无需额外 docs 文件） | index.js cmdDoctor |
| P-008 | 🟡 P2 | CLAUDE_HOOK_COMMAND 父链查找加入项目根标记（.devcodex/ \|\| package.json）防止 monorepo 跨层误命中 | index.js L298-311 |

## 二、工程层（1 文件）

### 2.1 `index.js`

- **P-008**：`CLAUDE_HOOK_COMMAND` 父链查找在匹配 `.claude/hooks/_runtime/lifecycle.cjs` 之外，新增项目根标记同层共存要求（`.devcodex/` 或 `package.json`），仅当二者同层存在才 require Hook，避免在 monorepo 中误激活外层工作区的 DevCodex Hook。
- **cmdDoctor**（新增）：基于 env 推断 platform（claude / jetbrains-copilot / vscode-copilot / copilot），调用 detectAgent 输出 agent，列出 install artifacts（CLAUDE.md / .claude/hooks / .github/copilot-instructions.md / .github/instructions / .github/hooks / .devcodex/profile），推荐执行 mode（hook-enforced vs instruction-fallback），并在 jetbrains-copilot/instruction-fallback 场景下打印 5 步验证清单（Use Instruction Files 开关、copilot-instructions 引用、applyTo 语义、CP gate 软门禁、pre-commit hook 接线）。
- 入口 switch 新增 `doctor` case；help 文本同步追加；module.exports 暴露 cmdDoctor。

## 三、回归门禁

- `node scripts/validate.js`：V1~V11 全 ✅，V4 versions aligned at 1.9.7
- `node scripts/test-hooks-runtime.js`：v1.9.6 用例继承全部通过
- `node index.js doctor`：本地输出预期 platform/agent/mode/artifacts/checklist

## 四、关联

- 输入：`changelogs/v1.9.6.md` §五（延后项 P-001/P-004/P-008）
- 上游：`changelogs/v1.9.6.md`
- 下一轮候选：JetBrains Workspace Hooks 官方支持后再回收 P-001 硬门禁等价能力
