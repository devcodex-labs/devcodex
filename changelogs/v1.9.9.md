# v1.9.9 — 单源分发回归修复 + 发布门禁补强

> **发布日期**: 2026-05-21  
> **类型**: PATCH  
> **关联报告**: `.devcodex/reports/audit/claude-code/20260521/08--全方面工作流深度复审.md`

---

## 🐛 Fixed

- 修复 `package.json` `files` 未包含 `instructions.md` 的发布回归，确保 npm tarball 中包含 v1.9.8 单源规范文件。
- 修复发布前验证只检查 forbidden content、不检查 required assets 的盲点；`scripts/validate.js` V6 与 `scripts/test-pack-clean.js` 均新增 `instructions.md`、`plugin.json`、hooks runtime、icon 等关键资产正向断言。
- 修复 `scripts/validate.js` V8 仍以仓内 `CLAUDE.md` 作为父链 `CLAUDE.md` 比对源的旧逻辑；v1.9.8+ 后父链 `CLAUDE.md` 应与单源 `instructions.md` 对齐。
- 同步 README / RULES / profile 中关于 Copilot 端 `.github/agents/` 默认分发恢复、源码单源 `instructions.md`、Skill 数量与当前版本阶段的说明。
- 将 `instructions.md` / `CLAUDE.md` 中 Claude-only 的合规索引说明改为平台中性表述，避免该内容传播到 Copilot 端 `.github/copilot-instructions.md`。
- 更新 `skills/audit-common/SKILL.md` 的 V8 覆盖说明，去除已过期的“8 文件抽样 / 33%”口径。

## ✅ 验证

- `node scripts/test-pack-clean.js`
- `node scripts/validate.js`
- `node scripts/validate-versions.js`
- `node scripts/test-hooks-runtime.js`
- `npm pack --dry-run --json`：确认 `instructions.md` 存在、`copilot-instructions.md` 不存在、`agents/devcodex.agent.md` 存在

## 影响范围

- npm / npx 安装后的 `devcodex init` 与 `devcodex update` 单源复制链路恢复可用。
- Copilot 端继续默认分发 `.github/agents/`；Claude Code 端继续通过 Skills 路由，不分发 agents。
- 无 Breaking Change。