# 违规登记表（template）

> **文件定位**：模板。`devcodex init` / `devcodex init --claude` 会将本文件复制到目标项目运行时 `data/violations.md`（Copilot: `.github/data/violations.md`；Claude Code: `.claude/data/violations.md`）。
> **使用**：在**用户项目内**，由 AI 按 `17-compliance` 规则追加 VL-001 起的违规记录。
> **维护者自身的违规记录**：不在本包内分发；启用 `workspace-namespace` 时写入对应 active-root，例如 `<工作区根>/.devcodex/<project>/data/violations.md`。

---

## 字段规范

| 字段 | 说明 | 必填 |
|------|------|:----:|
| 编号 | `VL-NNN`（三位数，从 001 起）| 是 |
| 规范化意图 | 固定为 `record.violation` | 是 |
| 置信度 | 高 / 中 / 低（低置信度不得写入）| 是 |
| 登记时间 | 精确到分钟，格式 `YYYY-MM-DD HH:mm` | 是 |
| 规范 | 违反的规范编号（如 `S03` / `C11` / `FC4`）| 是 |
| 现象 | 一句话描述违规现象 | 是 |
| 根因 | 规范缺陷 / AI 判断失误 / 用户显式越权 | 是 |
| 是否已修复 | 是 / 否 / 部分 | 是 |
| 修复方案 | 对应修复批次或处置策略 | 是 |
| 修复时间 | 完成修复时间，未完成为 `—` | 是 |
| 是否已验证 | 是 / 否 / 部分 | 是 |
| 验证时间 | 完成验证时间，未完成为 `—` | 是 |
| 验证证据 | 命令、报告、SCV/ECR 或 CRS 证据 | 是 |
| 升级关联 | 关联 PF/GAP/ISSUE 编号，无则 `—` | 否 |
| 关闭时间 | 关闭时间，未关闭为 `—` | 是 |
| 状态 | 🔄 / ✅ / ⚠️ | 是 |

---

## 登记表

| 编号 | 登记时间 | 规范化意图 | 置信度 | 规范 | 现象 | 根因 | 是否已修复 | 修复方案 | 修复时间 | 是否已验证 | 验证时间 | 验证证据 | 升级关联 | 关闭时间 | 状态 |
|------|----------|--------------|--------|------|------|------|:----------:|----------|----------|:----------:|----------|----------|----------|----------|:----:|
| VL-000 | YYYY-MM-DD HH:mm | record.violation | 高 | 示例 | EXAMPLE — 删除本行 | EXAMPLE | 否 | 示例修复方案 | — | 否 | — | — | — | — | 🔄 |
