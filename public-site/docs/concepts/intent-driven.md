# 意图驱动

## 你在问什么

为什么同样一句话，有时只出分析，有时会改文件？因为 DevCodex 先判断任务意图，再选工作流，而不是只匹配关键词。

## 心智模型

意图回答四件事：你要完成什么、作用在哪个项目、允许多大风险、这次能不能改文件。

工作流是边界，不是标签：`analyze` 保持只读，`fix` 和 `dev` 会进入确认后的修改，`chat` 不走项目执行链。

## 示例

「告诉我测试为什么红，不要改」→ `analyze` 或 `audit`。  
「复现并修好这个失败用例」→ `fix`。  
「继续上次那个需求」→ `resume`，沿用原来的工作流。

## 边界

- 含糊请求可能先被收成规划或要求你补范围，不会偷偷改仓库。
- `plan` 是阶段或能力，不是第九个工作流。
- 自动入口 `@devcodex-auto` / `@rocky` 不会扩大删除、发布或越界权限。

## 相关页

[架构怎么跑](/concepts/architecture) · [工作流总览](/workflows/) · [常见任务](/guide/common-tasks)
