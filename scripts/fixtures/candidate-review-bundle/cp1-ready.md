# CP1 Candidate

phaseKind: CP1
schemaVersion: CandidateReviewBundleV1

## RQMatrix

RQ-1~RQ-8 all evaluated with evidence.

## DomainRealityMatrix

DistributionRequirementRealityGate covers relation, package channel, license, command surface, runtime capability and phaseKind.

## ClaimEvidenceMatrix

Every requirement claim has repo evidence or an explicit unverified boundary.

## EscapeAbsorptionQueue

External findings are queued with disposition instead of being absorbed directly.
