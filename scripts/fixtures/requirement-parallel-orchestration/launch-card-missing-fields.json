{
  "name": "launch-card-missing-fields",
  "card": {
    "schemaVersion": "ParallelLaunchCardV1",
    "requirementId": "REQ-MISSING",
    "displayName": "缺字段卡片",
    "activeRoot": "E:/Worker/.devcodex/devcodex-v1",
    "sessionPrompt": "Only work on this task.",
    "isolationMode": "same-active-root-disjoint-paths",
    "mergeProtocol": {
      "schemaVersion": "IntegrationMergeProtocolV1",
      "mergeOrder": ["REQ-MISSING"],
      "conflictChecks": ["git diff --name-only"],
      "validationRoute": ["npm run test:changed"],
      "reportMemoryOwner": "main-session-single-writer",
      "failureAction": "stop"
    },
    "validationRoute": ["npm run test:changed"],
    "stopCondition": "Stop on forbidden shared surfaces."
  },
  "expected": {
    "classification": "launch-card-invalid",
    "missingFields": ["allowedPaths", "forbiddenSharedSurfaces"]
  }
}
