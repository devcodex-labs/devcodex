# Retired workspace skill route fixtures

These files preserve the pre-B4 `WorkspaceSkillAutoMatch`,
`WorkspaceSkillIntent`, and time-window retirement implementations for
historical review only.

They are not runtime modules, package files, CLI commands, validation nodes,
or release gates. The active implementation is the unified staged
`skill_route` path under `hooks/_runtime`.
