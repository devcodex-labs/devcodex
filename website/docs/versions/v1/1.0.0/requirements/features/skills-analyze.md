# 需求：skills/analyze/

> ⚠️ 历史快照说明：本页保留 `1.0.0` 阶段的 analyze Skill 需求。当时文档中的层级字段是历史规划；当前版本 `token-check` 仅为授权占位，analyze 子能力全量开放。

**状态**：⬜ 待开发  
**优先级**：P2  
**参考**：原 v0.03 `skills/analyze/`（目录已删除，当前实现见 `skills/analyze-*/`）

## 文件列表

> ⚠️ **官方结构要求**：扁平一级目录，`name` 与文件夹名一致。

| Skill | 路径（官方扁平结构）| `1.0.0` 阶段规划层级 |
|-------|---------------------|----------------------|
| `analyze-research` | `skills/analyze-research/SKILL.md` | Free |

## 核心需求

- 只读工作流，禁止文件修改
- 每条结论必须附三项验证（合理性/可实施性/收益）
